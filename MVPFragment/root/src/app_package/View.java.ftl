package ${packageName}.${viewPackage};

import ${packageName}.base.IView;

/**
 * package: ${packageName}.${viewPackage}.${viewClass}
 * author: gyc
 * description: 
 * time: create at ${.now?date} ${.now?time}
 */
public interface ${viewClass} extends IView {
    
}
