package ${packageName}.${presenterPackage};

import ${packageName}.base.BasePresenter;
import ${packageName}.${viewPackage}.${viewClass};

/**
 * package: ${packageName}.${presenterPackage}.${presenterClass}
 * author: gyc
 * description: 
 * time: create at ${.now?date} ${.now?time}
 */
public class ${presenterClass} extends BasePresenter<${viewClass}> {
    
}
